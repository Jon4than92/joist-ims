<%= form_with(model: custodian, local: true) do |form| %>
  <% if custodian.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(custodian.errors.count, "error") %> prohibited this custodian from being saved:</h2>

      <ul>
      <% custodian.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
